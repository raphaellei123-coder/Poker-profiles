// Example firebase config file. Create a Firebase project and replace the placeholders below.
// Then create a file at js/firebase-config.js with the same structure (do NOT commit real secrets here if you want privacy).

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};
